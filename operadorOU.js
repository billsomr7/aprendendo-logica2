/* let temIngresso = true
let nomeNaLista = true

if (temIngresso || nomeNaLista) {
    alert("Pode entrar no show!")
} else {
    alert("Não pode entrar no show!")
 } */

    let temIngresso = false
    let nomeNaLista = false 
                 /* OU */
    if (temIngresso || nomeNaLista) {
        alert("Pode entrar no show!")
    } else {
        alert("Não pode entrar no show!")
     }